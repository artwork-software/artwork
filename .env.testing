APP_NAME=Artwork
APP_ENV=testing
APP_KEY=base64:O0Gz48aV7indM1WtVFnEGcl6tAIMHOe7X2HQc+ohdms=
APP_DEBUG=true
APP_URL="https://artwork.ddev.site"

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=artwork_test
DB_USERNAME=db
DB_PASSWORD=db

BCRYPT_ROUNDS=4

CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
SESSION_LIFETIME=120
MAIL_MAILER=array

SCOUT_DRIVER=null
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_KEY=ddev
SCOUT_QUEUE=false

REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

BROADCAST_DRIVER=log
BROADCAST_CONNECTION=log

PUSHER_PORT=80
VITE_PUSHER_PORT="${PUSHER_PORT}"
LARAVEL_WEBSOCKETS_PORT="${PUSHER_PORT}"
PUSHER_SCHEME=http
PUSHER_ENCRYPT=false
PUSHER_APP_KEY='app-key'
PUSHER_APP_ID='app-id'
PUSHER_APP_SECRET='app-secret'
PUSHER_APP_CLUSTER=mt1
PUSHER_HOST=artwork.ddev.site

REVERB_APP_ID="${PUSHER_APP_ID}"
REVERB_APP_KEY="${PUSHER_APP_KEY}"
REVERB_APP_SECRET="${PUSHER_APP_SECRET}"
REVERB_HOST="${PUSHER_HOST}"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# Backup
BACKUP_ENABLED=false

DEBUGBAR_ENABLED=false
TELESCOPE_ENABLED=false
CLOCKWORK_ENABLE=false

SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.0
SAGE_API_ENABLED=false

USE_CHAT_MODULE=false

SYSTEM_MAIL=noreply@artwork.software
FALLBACK_PAGE_TITLE=Artwork

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false
